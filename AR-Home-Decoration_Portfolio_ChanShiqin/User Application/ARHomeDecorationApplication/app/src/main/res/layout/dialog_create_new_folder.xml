<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@android:color/white"
    android:layout_gravity="center">

    <!-- Dialog Title -->
    <TextView
        android:id="@+id/dialogCreateFolderTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Create New Folder"
        android:textColor="#333333"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp" />

    <!-- Folder Name Input -->
    <EditText
        android:id="@+id/folderNameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Folder Name"
        android:textColor="#333333"
        android:textColorHint="#333333"
        android:background="@drawable/edit_text_bg"
        android:padding="12dp"
        android:textSize="16sp"
        android:layout_marginBottom="16dp"
        android:inputType="text" />

    <!-- Error Message TextView (Hidden by default) -->
    <TextView
        android:id="@+id/errorMessageText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Folder name cannot be empty!"
        android:textColor="#F44336"
        android:textSize="14sp"
        android:visibility="gone"
        android:layout_marginBottom="16dp" />

    <!-- Create Button -->
    <Button
        android:id="@+id/btnCreateFolder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Create Folder"
        android:textColor="@android:color/white"
        android:backgroundTint="#4CAF50"
        android:textSize="15sp"
        android:textAllCaps="false"
        android:layout_marginBottom="8dp" />

    <!-- Cancel Button -->
    <Button
        android:id="@+id/btnCancelFolderCreation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cancel"
        android:textColor="@android:color/white"
        android:backgroundTint="#FF6A6A"
        android:textSize="15sp"
        android:textAllCaps="false" />
</LinearLayout>
